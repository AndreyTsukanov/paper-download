"""grab — OA-first downloader for a specific academic paper.

Deterministic Python core (reuses paper_search_mcp connectors directly) with a
Haiku disambiguation fallback used only when a title match is genuinely ambiguous.
"""
